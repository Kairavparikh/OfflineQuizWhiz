# Core dependencies for MCQ Generator

# DOCX parsing
python-docx==1.1.2

# HTTP requests for LLM API calls
requests==2.31.0

# Data validation and serialization
pydantic==2.10.4  # Optional: for advanced validation if needed

# Phase 3 dependencies (PDF parsing, multimodal)
PyMuPDF==1.24.0  # PDF parsing with image extraction
Pillow==11.0.0  # Image processing

# Phase 4 dependencies (Web UI)
fastapi==0.115.6  # API server
uvicorn[standard]==0.34.0  # ASGI server with WebSocket support
aiofiles==24.1.0  # Async file operations for FastAPI
python-multipart==0.0.20  # For file uploads (future enhancement)

# Optional: Excel export
openpyxl==3.1.5  # Excel file export
